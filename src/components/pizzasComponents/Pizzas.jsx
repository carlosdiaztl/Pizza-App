import React from 'react'
import Header from '../header/Header';

const Pizzas = ({name}) => {
  console.log(name);
  
  return (

    <div>
    
    
    
    buscate la pizza {name} </div>
  )
}

export default Pizzas